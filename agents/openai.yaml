interface:
  display_name: "Multi-Agent Dispatcher"
  short_description: "智能判断是否需要多 Agent，并在长任务中编排、续跑与验收"
  default_prompt: "Use $multi-agent-dispatcher to decide whether this task warrants multi-agent dispatch; if it does, split it into bounded agents, track progress, verify evidence, and converge the result."
