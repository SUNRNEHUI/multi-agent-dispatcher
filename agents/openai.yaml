interface:
  display_name: "Multi-Agent Dispatcher"
  short_description: "多 Agent 长任务编排、状态续跑、验证证据与结果收敛"
  default_prompt: "Use $multi-agent-dispatcher to split this large task into bounded agents, track progress, verify evidence, and converge the result."
